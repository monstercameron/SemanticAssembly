sasm build refused the file:
error E-ISA-OPCODE swapIfLessOrEqual: unknown operation 'BranchGreaterThan'
error E-CFG-EDGE PartitionBody: declares `terminates branch` but the last row (swapIfLessOrEqual) is fallthrough
error E-CFG-LAYOUT PartitionBody: falls through without a terminator but declares 2 successors — exactly one (the physically next block) is required
error E-CFG-EDGE PartitionBody: declares `successor AdvanceScan` but the terminator cannot take that edge — a stale edge silently widens every dataflow merge
error E-CFG-EDGE AdvanceScan: declares predecessors ['PartitionBody', 'SwapElements'] but the CFG computes ['SwapElements'] — `predecessor` must be the exact inverse of `successor` when present