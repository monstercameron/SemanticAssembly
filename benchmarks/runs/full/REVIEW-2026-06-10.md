# External critique of the D1 study (2026-06-10) — dispositions

Eight findings against the in-flight full study. Each recorded with its
disposition; results.md must cite this file. Verbatim bottom line accepted:
**"The implementation is credible; the thesis is not yet earned."**

| # | finding | verdict | disposition |
|---|---------|---------|-------------|
| 1 | Current evidence is anti-thesis, not merely incomplete | ACCEPT (with reframe) | results.md must state: small tasks saturate (consistent with §16.1's size prediction, so not *dis*confirming), the spill task shows a real arm-b authoring tax, and the F3 "stale local facts actively harm (d)" sub-claim — DESIGN's easiest-to-falsify — was tested and **fell**. No thesis claim from this dataset. |
| 2 | Protocol 2 reported before it exists | ACCEPT — fixed | `report()` now prints rounds-present and renames the column "(no feedback rounds run yet — column equals P1)" until rounds >1 exist. Also recorded: our P2 reuses round-1 attempts as its first round (cost decision), deviating from DESIGN's run-separately spec. |
| 3 | "Same information" across arms is not true | ACCEPT — the most substantive | Arms c/d carry a curated subset (reads/writes/liveOut/saves/restores/purpose), not CFG/effects/regions/types. Claim downgraded to "mechanically derived subset of the same source." Cannot regenerate arms mid-run without invalidating collected cells; a full-fact export mode is REQUIRED before any rerun, and b-vs-d deltas in THIS dataset cannot be attributed to checkability alone. |
| 4 | Scoring state can silently go stale | ACCEPT as hardening | `report()` now warns on any candidate file missing from scores.json. (Observed gap was the in-flight window between filler writes and the next score pass.) |
| 5 | Prompt parity confounded (b primer ~22k vs a ~1.8k) | ACCEPT, partially irreducible | The primer is part of the format's real cost, but teaching load and format effect are not separable here. results.md must report per-arm prompt sizes and scope b-vs-d conclusions accordingly. |
| 6 | F1 has no discriminatory power | ACCEPT | All arms 100%. v2 needs multi-hop/what-if questions and larger functions. The saturated family still supports one claim: no arm *hurts* comprehension at this size. |
| 7 | Fable/Sonnet split blocks size-sweep claims | ACCEPT (pre-documented in STATUS.md) | fib-vs-quicksort comparisons support no size-widening claim in this run. |
| 8 | README overreach ("every hidden assumption") | ACCEPT — fixed | Headline now scopes to the enumerated vocabulary and names the open A1 oracle. |

## Standing consequences for the write-up

results.md conclusions section may claim at most:
- the harness works end-to-end and the bar is enforced uniformly;
- arm b pays a measurable authoring tax (spill idiom) one-shot;
- stale inline comments did not propagate at this scale (sub-claim falsified);
- arm b's failure mode is loud (validator names the row), arm d's lie ships
  silently — an artifact-state difference, not a behavioral one;
- everything else awaits: full-fact arms, harder F1, single-model rerun,
  genuine Protocol-2 rounds.
